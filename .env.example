# 复制本文件为 .env 并填写真实值（.env 不会上传到 GitHub）
# 本地开发可不填 DATABASE_URL，会自动使用 data/db.json 文件

# Supabase Postgres 连接串（线上部署时填）
# 在 Supabase 控制台 → Project Settings → Database → Connection string → URI 复制
DATABASE_URL=

# 端口（Render 会自动注入 PORT，本地默认 5200）
PORT=5200
